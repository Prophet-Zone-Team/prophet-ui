{
  "name": "prophet-mobile",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "cap:sync": "cap sync ios",
    "cap:sync:production": "CAPACITOR_ENV=production cap sync ios",
    "cap:sync:staging": "CAPACITOR_ENV=staging cap sync ios",
    "cap:sync:development": "CAPACITOR_ENV=development cap sync ios",
    "cap:sync:local": "CAPACITOR_ENV=development cap sync ios",
    "cap:ios": "cap open ios"
  },
  "dependencies": {
    "@capacitor/app": "^7.0.1",
    "@capacitor/browser": "^7.0.1",
    "@capacitor/core": "^7.2.0",
    "@capacitor/ios": "^7.2.0",
    "@capacitor/network": "^7.0.1",
    "@capacitor/splash-screen": "^7.0.1",
    "@capacitor/status-bar": "^7.0.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.2.0",
    "typescript": "^5.7.3"
  }
}
