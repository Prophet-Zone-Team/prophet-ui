# Do not set node-options here — pnpm applies it to every spawned Node process
# (including Next.js webpack workers), which can exceed Vercel's ~8GB RAM limit.
# Heap limits live in package.json scripts instead (build vs cf:build).
